openapi: "3.0.3"
info:
  title: "pulsar_backend API"
  description: "pulsar_backend API"
  version: "1.0.0"
servers:
  - url: "https://pulsar_backend"
paths:
  /:
    get:
      description: ""
      responses:
        "200":
          description: "OK"
          content:
            text/plain:
              schema:
                type: "string"
              examples:
                Example#1:
                  value: "Hello World!"
  /*:
    get:
      description: ""
      parameters:
        - name: "*"
          in: "path"
          required: true
          schema:
            type: "string"
      responses:
        "404":
          description: "Not Found"
          content:
            text/plain:
              schema:
                type: "string"
              examples:
                Example#1:
                  value: "404 Not Found"